<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Pebble Beach Weekend · April 10–12</title>
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,700;1,400;1,700&family=Cormorant+Garamond:ital,wght@0,300;0,400;1,300;1,400&family=Space+Mono:wght@400;700&display=swap" rel="stylesheet">
<style>
  :root {
    --sand: #f2ede4;
    --fog: #ddd9d0;
    --ocean: #2a4a5e;
    --deep: #1a2e3b;
    --olive: #77ab3c;
    --tide: #7fa8b8;
    --blush: #c9a882;
    --poppy: #f28627;
    --cream: #faf7f2;
    --ink: #1c1c1c;
    --twilight: #b8c4d4;
    --lavender: #cab2d4;
    --terracotta: #8b4a2e;
    --sage: #7a8c6e;
    --stone: #c4a882;
    --dusk-blue: #6a7d9e;
  }

  * { margin: 0; padding: 0; box-sizing: border-box; }
  html { scroll-behavior: smooth; }

  body {
    background: var(--cream);
    color: var(--ink);
    font-family: 'Cormorant Garamond', serif;
    overflow-x: hidden;
  }

  /* ─── HERO ─── */
  .hero {
    min-height: 100vh;
    background: var(--deep);
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }

  .hero-waves {
    position: absolute;
    bottom: 0; left: 0; right: 0;
    height: 200px;
    opacity: 0.15;
    width: 100%;
  }

  .stars {
    position: absolute;
    inset: 0;
    overflow: hidden;
  }

  .star {
    position: absolute;
    background: white;
    border-radius: 50%;
    animation: twinkle var(--d, 3s) ease-in-out infinite alternate;
  }

  @keyframes twinkle {
    from { opacity: 0.1; transform: scale(0.8); }
    to { opacity: 0.9; transform: scale(1.2); }
  }

  .horizon {
    position: absolute;
    bottom: 0; left: 0; right: 0;
    height: 40%;
    background: linear-gradient(to top, rgba(42,74,94,0.8), transparent);
  }

  .sparkle {
    position: fixed;
    pointer-events: none;
    border-radius: 50%;
    z-index: 9999;
    animation: sparkleFade var(--life, 0.8s) ease forwards;
  }

  @keyframes sparkleFade {
    0%   { opacity: 1; transform: translate(-50%,-50%) scale(1) rotate(0deg); }
    100% { opacity: 0; transform: translate(-50%,-50%) scale(0) rotate(200deg); }
  }

  .hero-content {
    text-align: center;
    z-index: 10;
    padding: 2rem;
    animation: heroFadeIn 1.5s ease forwards;
  }

  @keyframes heroFadeIn {
    from { opacity: 0; transform: translateY(30px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .hero-eyebrow {
    font-family: 'Space Mono', monospace;
    font-size: 0.7rem;
    letter-spacing: 0.3em;
    color: var(--tide);
    text-transform: uppercase;
    margin-bottom: 1.5rem;
  }

  .hero-title {
    font-family: 'Playfair Display', serif;
    font-size: clamp(3.5rem, 10vw, 8rem);
    font-weight: 400;
    color: var(--sand);
    line-height: 0.9;
    margin-bottom: 0.3em;
  }

  .hero-title em {
    font-style: italic;
    color: var(--blush);
  }

  .hero-subtitle {
    font-family: 'Cormorant Garamond', serif;
    font-style: italic;
    font-size: clamp(1.2rem, 3vw, 1.8rem);
    color: var(--fog);
    font-weight: 300;
    margin-bottom: 2.5rem;
    letter-spacing: 0.05em;
  }

  .hero-date {
    font-family: 'Space Mono', monospace;
    font-size: 0.75rem;
    letter-spacing: 0.25em;
    color: var(--tide);
    border: 1px solid rgba(127,168,184,0.3);
    display: inline-block;
    padding: 0.7rem 2rem;
    text-transform: uppercase;
  }

  .scroll-hint {
    position: absolute;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
    color: var(--tide);
    font-family: 'Space Mono', monospace;
    font-size: 0.6rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    animation: scrollBounce 2s ease-in-out infinite;
    z-index: 10;
  }

  .scroll-hint::after {
    content: '';
    width: 1px;
    height: 30px;
    background: var(--tide);
    opacity: 0.5;
  }

  @keyframes scrollBounce {
    0%, 100% { transform: translateX(-50%) translateY(0); }
    50% { transform: translateX(-50%) translateY(6px); }
  }

  /* ─── SECTIONS ─── */
  section {
    padding: 6rem 2rem;
    max-width: 900px;
    margin: 0 auto;
  }

  .section-label {
    font-family: 'Space Mono', monospace;
    font-size: 0.65rem;
    letter-spacing: 0.3em;
    text-transform: uppercase;
    color: var(--stone);
    margin-bottom: 1rem;
  }

  .section-title {
    font-family: 'Playfair Display', serif;
    font-size: clamp(2rem, 5vw, 3.5rem);
    font-weight: 400;
    color: var(--ocean);
    line-height: 1.1;
    margin-bottom: 1.5rem;
  }

  .section-title em { font-style: italic; }

  .section-body {
    font-size: 1.25rem;
    line-height: 1.8;
    color: #3a3a3a;
    font-weight: 300;
    max-width: 640px;
  }

  /* ─── DIVIDER ─── */
  .divider {
    width: 100%;
    max-width: 900px;
    margin: 0 auto;
    height: 1px;
    background: linear-gradient(to right, transparent, var(--twilight), transparent);
    opacity: 0.4;
  }

  /* ─── WISH SECTION ─── */
  .wish-section {
    background: linear-gradient(160deg, var(--deep) 60%, #2a2040 100%);
    color: var(--sand);
    padding: 6rem 2rem;
    position: relative;
    overflow: hidden;
  }

  .wish-section::before {
    content: '✦';
    position: absolute;
    font-size: 20rem;
    color: rgba(255,255,255,0.02);
    top: -3rem;
    right: -2rem;
    line-height: 1;
    pointer-events: none;
  }

  .wish-inner { max-width: 900px; margin: 0 auto; }
  .wish-section .section-label { color: var(--blush); }
  .wish-section .section-title { color: var(--sand); }
  .wish-section .section-body { color: var(--fog); font-size: 1.3rem; }

  .wish-examples {
    font-family: 'Cormorant Garamond', serif;
    font-style: italic;
    font-size: clamp(1.2rem, 3vw, 1.6rem);
    color: var(--lavender);
    line-height: 1.6;
    margin-top: 2.5rem;
    padding-left: 2rem;
    border-left: 2px solid var(--lavender);
    max-width: 600px;
    opacity: 0.9;
  }

  /* ─── LOCATION ─── */
  .location-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3rem;
    margin-top: 3rem;
    align-items: start;
  }

  @media (max-width: 600px) {
    .location-grid { grid-template-columns: 1fr; }
  }

  .location-detail {
    border-top: 1px solid var(--fog);
    padding-top: 1.2rem;
  }

  .location-detail h4 {
    font-family: 'Space Mono', monospace;
    font-size: 0.65rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--blush);
    margin-bottom: 0.5rem;
  }

  .location-detail p {
    font-size: 1.1rem;
    line-height: 1.6;
    color: #3a3a3a;
    font-style: italic;
  }

  /* ─── SCHEDULE ─── */
  .schedule-section {
    background: linear-gradient(180deg, var(--sand) 0%, #ede8e0 100%);
    padding: 6rem 2rem;
  }

  .schedule-inner { max-width: 900px; margin: 0 auto; }

  .days-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: 2rem;
    margin-top: 3rem;
  }

  .day-card {
    background: var(--cream);
    padding: 2.5rem 2rem;
    position: relative;
    overflow: hidden;
    transition: transform 0.3s ease;
  }

  .day-card:hover { transform: translateY(-4px); }

  .day-card::before {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 3px;
  }

  .day-card:nth-child(1)::before { background: linear-gradient(90deg, var(--dusk-blue), var(--twilight)); }
  .day-card:nth-child(2)::before { background: linear-gradient(90deg, var(--terracotta), var(--poppy)); }
  .day-card:nth-child(3)::before { background: linear-gradient(90deg, var(--sage), var(--olive)); }

  .day-number {
    font-family: 'Playfair Display', serif;
    font-size: 3.5rem;
    font-weight: 700;
    color: var(--fog);
    line-height: 1;
    margin-bottom: 0.2rem;
  }

  .day-name {
    font-family: 'Space Mono', monospace;
    font-size: 0.65rem;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: var(--blush);
    margin-bottom: 1.5rem;
  }

  .day-events {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 0.9rem;
  }

  .day-event {
    display: flex;
    gap: 1rem;
    align-items: flex-start;
    font-size: 1rem;
    line-height: 1.5;
    color: #3a3a3a;
  }

  .event-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--blush);
    margin-top: 0.5rem;
    flex-shrink: 0;
  }

  .event-time {
    font-family: 'Space Mono', monospace;
    font-size: 0.65rem;
    color: var(--blush);
    letter-spacing: 0.05em;
    white-space: nowrap;
    margin-top: 0.2rem;
    flex-shrink: 0;
  }

  .event-text { font-style: italic; }
  .event-sub {
    font-size: 0.85rem;
    color: #888;
    font-style: normal;
    display: block;
    margin-top: 0.1rem;
  }

  /* ─── FOOTER ─── */
  footer {
    background: linear-gradient(160deg, var(--deep) 50%, #1a1520 100%);
    color: var(--fog);
    text-align: center;
    padding: 4rem 2rem;
  }

  footer .big-text {
    font-family: 'Playfair Display', serif;
    font-style: italic;
    font-size: clamp(2rem, 6vw, 4rem);
    color: var(--sand);
    margin-bottom: 1rem;
  }

  footer p {
    opacity: 0.6;
    font-family: 'Space Mono', monospace;
    font-size: 0.7rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
  }

  /* ─── SCROLL ANIMATIONS ─── */
  .fade-up {
    opacity: 0;
    transform: translateY(40px);
    transition: opacity 0.8s ease, transform 0.8s ease;
  }

  .fade-up.visible {
    opacity: 1;
    transform: translateY(0);
  }

  .fade-up:nth-child(2) { transition-delay: 0.1s; }
  .fade-up:nth-child(3) { transition-delay: 0.2s; }

  /* ─── HOUSE PHOTO ─── */
  .house-photo {
    margin-top: 2.5rem;
    position: relative;
  }

  .house-photo img {
    width: 100%;
    max-height: 480px;
    object-fit: cover;
    display: block;
    filter: saturate(0.95) brightness(1.02);
  }

  .photo-caption {
    font-family: 'Space Mono', monospace;
    font-size: 0.62rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--blush);
    margin-top: 0.8rem;
    text-align: right;
  }

  /* ─── COLLAPSIBLE SECTIONS ─── */
  details > summary {
    list-style: none;
    cursor: pointer;
    position: relative;
    padding-right: 2.5rem;
  }

  details > summary::-webkit-details-marker { display: none; }

  details > summary::after {
    content: '';
    position: absolute;
    right: 0.5rem;
    top: 50%;
    width: 30px;
    height: 30px;
    border-left: 2px solid var(--poppy);
    border-bottom: 2px solid var(--poppy);
    transform: translateY(-70%) rotate(-45deg);
    transition: transform 0.3s ease, opacity 0.3s ease;
    opacity: 0.5;
  }

  details[open] > summary::after {
    transform: translateY(-30%) rotate(135deg);
    opacity: 0.7;
  }

  details > summary:hover::after { opacity: 1; }

  details > *:not(summary) {
    animation: slideDown 0.35s ease;
  }

  @keyframes slideDown {
    from { opacity: 0; transform: translateY(-8px); }
    to   { opacity: 1; transform: translateY(0); }
  }

  /* pulsing dot under arrow */
  details > summary::before {
    content: '';
    position: absolute;
    right: calc(0.5rem + 12px);
    top: calc(50% + 24px);
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--lavender);
    animation: dotPulse 1.8s ease-in-out infinite;
  }

  details[open] > summary::before { display: none; }

  @keyframes dotPulse {
    0%, 100% { transform: scale(1); opacity: 0.5; }
    50%       { transform: scale(1.6); opacity: 1; }
  }

  /* ─── ORBS ─── */
  .orb {
    position: fixed;
    border-radius: 50%;
    filter: blur(120px);
    pointer-events: none;
    z-index: 0;
    animation:
      orbFloat var(--dur, 20s) ease-in-out infinite alternate,
      orbBreath var(--bdur, 12s) ease-in-out infinite alternate;
  }

  @keyframes orbFloat {
    0%   { transform: translate(0, 0) scale(1); }
    100% { transform: translate(var(--tx, 40px), var(--ty, 30px)) scale(1.15); }
  }

  @keyframes orbBreath {
    0%   { opacity: 0.03; }
    100% { opacity: 0.12; }
  }

  @keyframes fadeInUp {
    from { opacity: 0; transform: translateY(20px); }
    to   { opacity: 1; transform: translateY(0); }
  }

  /* section label color variants */
  .section-label.sage  { color: var(--sage); }
  .section-label.terra { color: var(--terracotta); }

  .twilight-accent {
    border-left: 2px solid var(--twilight);
    padding-left: 1.5rem;
    color: var(--dusk-blue);
  }
</style>
</head>
<body>

<!-- ─── ENTRY SCREEN ─── -->
<div id="entry-screen" style="
  position: fixed; inset: 0; z-index: 99999;
  background: var(--deep);
  display: flex; flex-direction: column;
  align-items: center; justify-content: center;
  text-align: center; padding: 2rem;
  transition: opacity 1.5s ease;
">
  <p style="font-family:'Space Mono',monospace;font-size:0.65rem;letter-spacing:0.3em;color:var(--tide);text-transform:uppercase;margin-bottom:2rem;">you are invited</p>
  <h1 style="font-family:'Playfair Display',serif;font-size:clamp(2rem,6vw,4rem);color:var(--sand);font-weight:400;margin-bottom:0.5rem;">Who are you,</h1>
  <h1 style="font-family:'Playfair Display',serif;font-size:clamp(2rem,6vw,4rem);color:var(--blush);font-style:italic;font-weight:400;margin-bottom:3rem;">sweet friend?</h1>
  <input id="name-input" type="text" placeholder="your name..." autofocus style="background:transparent;border:none;border-bottom:1px solid rgba(127,168,184,0.4);color:var(--sand);font-family:'Cormorant Garamond',serif;font-size:1.5rem;text-align:center;padding:0.5rem 1rem;outline:none;width:280px;letter-spacing:0.05em;">
  <p id="entry-hint" style="font-family:'Space Mono',monospace;font-size:0.55rem;letter-spacing:0.2em;color:var(--tide);margin-top:1rem;opacity:0.6;">press enter</p>
  <p id="entry-error" style="font-family:'Space Mono',monospace;font-size:0.55rem;letter-spacing:0.2em;color:var(--poppy);margin-top:1rem;opacity:0;transition:opacity 0.3s;">hmm, i don't recognize you...</p>
</div>

<!-- ─── ORBS ─── -->
<div class="orb" style="width:500px;height:500px;background:#c4b8d0;top:-100px;left:-100px;--dur:25s;--tx:60px;--ty:50px;--bdur:16s;"></div>
<div class="orb" style="width:400px;height:400px;background:#f5f0ee;top:40%;right:-80px;--dur:18s;--tx:-50px;--ty:40px;--bdur:11s;"></div>
<div class="orb" style="width:350px;height:350px;background:#f0edf4;bottom:10%;left:30%;--dur:22s;--tx:30px;--ty:-60px;--bdur:14s;"></div>

<!-- ─── HERO ─── -->
<div class="hero">
  <div class="stars" id="stars"></div>
  <div class="horizon"></div>

  <div class="hero-content">
    <p class="hero-eyebrow">10-12 friends are invited</p>
    <h1 class="hero-title">Pebble Beach<br><em>Portal</em></h1>
    <p class="hero-subtitle">a weekend to get a little creative together</p>
    <div class="hero-date">April 10 — 12, 2026</div>
  </div>

  <div class="scroll-hint">scroll</div>

  <svg class="hero-waves" viewBox="0 0 1440 200" preserveAspectRatio="none" fill="white">
    <path d="M0,80 C240,140 480,20 720,80 C960,140 1200,20 1440,80 L1440,200 L0,200 Z"/>
    <path d="M0,120 C360,60 720,160 1080,100 C1260,70 1380,130 1440,120 L1440,200 L0,200 Z" opacity="0.5"/>
  </svg>
</div>

<!-- ─── INTRO ─── -->
<section class="fade-up">
  <details>
    <summary>
      <p class="section-label">The occasion</p>
      <h2 class="section-title">A nod to my 27th birthday,<br><em>a weekend for us all!</em></h2>
    </summary>
    <p class="section-body">
      Yes, my birthday is the catalyst for this — April 3rd — but this is really about carving out a weekend to be together. To hang out. To be goofy and silly. To make memories together!
    </p>
  </details>
</section>

<div class="divider"></div>

<!-- ─── WISH ─── -->
<section class="fade-up">
  <details>
    <summary>
      <p class="section-label">One birthday wish</p>
      <h2 class="section-title">I'm casting a spell<br><em>on all of you</em></h2>
    </summary>
    <p class="section-body">
      My one wish is this: I ask each of you to contribute something unique <em>~thing~</em> to the weekend, or more, if the spirit moves you. There are no rules, no limits. Simple or extravagant. Silly or sublime!
    </p>
    <p class="wish-examples">Examples include an edible contribution, music serenade, facilitated walk in nature, a performance of sorts, scavenger hunt...</p>
  </details>
</section>

<div class="divider"></div>

<!-- ─── LOCATION ─── -->
<section class="fade-up">
  <details>
    <summary>
      <p class="section-label">Where we're staying</p>
      <h2 class="section-title">A home<br><em>by the sea</em></h2>
    </summary>
    <p class="section-body">
      My parents have graciously offered their home for the weekend, and I cannot wait to welcome you through the door.
    </p>
    <div class="house-photo fade-up">
      <img src="https://placehold.co/600x400">
      <p class="photo-caption">home base for the weekend</p>
    </div>
    <div class="location-grid">
      <div class="location-detail fade-up">
        <h4>Sleeping arrangements</h4>
        <p>Sleeping logistics will be crafty. All of us will find a cozy spot — some beds, most on air mattresses. Sleepover! ... Pillow fight!?.</p>
      </div>
      <div class="location-detail fade-up">
        <h4>Arrival</h4>
        <p>Come whenever you'd like on Friday. The house opens its arms to you all day. The weekend together will "start" after 5:00 pm.</p>
      </div>
    </div>
  </details>
</section>

<div class="divider"></div>

<!-- ─── SCHEDULE ─── -->
<div class="schedule-section">
  <div class="schedule-inner">
    <p class="section-label" style="text-align:center">The weekend</p>
    <h2 class="section-title" style="text-align:center; max-width:600px; margin: 0 auto 0.5rem;">Three days,<br><em>no agenda — just a few anchors</em></h2>

    <div class="days-grid">

      <!-- Day 1 -->
      <div class="day-card fade-up">
        <div class="day-number">01</div>
        <div class="day-name">Friday · Apr 10</div>
        <ul class="day-events">
          <li class="day-event">
            <span class="event-dot"></span>
            <div>
              <span class="event-text">Welcome in, anytime 🤗</span>
              <span class="event-sub">Arrive whenever you can, the weekend together will "start" after 5:00 pm</span>
            </div>
          </li>
          <li class="day-event">
            <span class="event-time">7:30pm</span>
            <div>
              <span class="event-text">Dinner together</span>
            </div>
          </li>
          <li class="day-event">
            <span class="event-dot"></span>
            <div>
              <span class="event-text">Games 'n chill 🎲</span>
              <span class="event-sub">Bring your favorite, or make one up for us!</span>
            </div>
          </li>
        </ul>
      </div>

      <!-- Day 2 -->
      <div class="day-card fade-up">
        <div class="day-number">02</div>
        <div class="day-name">Saturday · Apr 11</div>
        <ul class="day-events">
          <li class="day-event">
            <span class="event-dot"></span>
            <div>
              <span class="event-text">Polar Bear Plunge 🧊</span>
              <span class="event-sub">Dive in or cheerlead — both are required</span>
            </div>
          </li>
          <li class="day-event">
            <span class="event-dot"></span>
            <div>
              <span class="event-text">Daytime adventure ideas ✨</span>
              <span class="event-sub">Monterey Bay Aquarium · Point Lobos Hike</span>
            </div>
          </li>
          <li class="day-event">
            <span class="event-time">7:00pm</span>
            <div>
              <span class="event-text">Dinner</span>
            </div>
          </li>
          <li class="day-event">
            <span class="event-dot"></span>
            <div>
              <span class="event-text">Beach bonfire 🔥</span>
              <span class="event-sub">Maybe an effigy situation? TBD.</span>
            </div>
          </li>
          <li class="day-event">
            <span class="event-dot"></span>
            <div>
              <span class="event-text">After dark ✨</span>
              <span class="event-sub">DJ · games · mind-altering magic · and whatever else appears</span>
            </div>
          </li>
        </ul>
      </div>

      <!-- Day 3 -->
      <div class="day-card fade-up">
        <div class="day-number">03</div>
        <div class="day-name">Sunday · Apr 12</div>
        <ul class="day-events">
          <li class="day-event">
            <span class="event-dot"></span>
            <div>
              <span class="event-text">Slow morning ☕️</span>
              <span class="event-sub">Coffee, lingering, no rushing</span>
            </div>
          </li>
          <li class="day-event">
            <span class="event-dot"></span>
            <div>
              <span class="event-text">One more spontaneous thing? 😏</span>
              <span class="event-sub">TBD by the energy of the room</span>
            </div>
          </li>
          <li class="day-event">
            <span class="event-dot"></span>
            <div>
              <span class="event-text">Leave whenever you'd like 💛</span>
              <span class="event-sub">Or, stay!</span>
            </div>
          </li>
        </ul>
      </div>

    </div>
  </div>
</div>

<!-- ─── FOOTER ─── -->
<footer>
  <p class="big-text">See you soon.</p>
  <p>Pebble Beach · April 10–12, 2026</p>
</footer>

<script>
// ── ENTRY SCREEN ──
const guests = ['aimee','cole','abby','vivi','rory','basile','lilja'];
const nameInput = document.getElementById('name-input');
const entryScreen = document.getElementById('entry-screen');
const entryError = document.getElementById('entry-error');

nameInput.addEventListener('keydown', (e) => {
  if (e.key === 'Enter') {
    const val = nameInput.value.trim().toLowerCase();
    if (guests.includes(val)) {
      const displayName = val.charAt(0).toUpperCase() + val.slice(1);
      entryScreen.style.transition = 'none';
      entryScreen.innerHTML = `
        <p style="font-family:'Space Mono',monospace;font-size:0.65rem;letter-spacing:0.3em;color:var(--tide);text-transform:uppercase;margin-bottom:2rem;opacity:0;animation:fadeInUp 1s ease 0.3s forwards;">a gift for you</p>
        <h1 style="font-family:'Playfair Display',serif;font-size:clamp(2rem,6vw,4rem);color:var(--sand);font-weight:400;opacity:0;animation:fadeInUp 1s ease 0.6s forwards;">welcome,</h1>
        <h1 style="font-family:'Playfair Display',serif;font-size:clamp(2rem,6vw,4rem);color:var(--blush);font-style:italic;font-weight:400;opacity:0;animation:fadeInUp 1s ease 0.9s forwards;">${displayName}</h1>
      `;
      setTimeout(() => {
        entryScreen.style.transition = 'opacity 1.5s ease';
        entryScreen.style.opacity = '0';
        setTimeout(() => entryScreen.remove(), 1500);
      }, 3000);
    } else {
      entryError.style.opacity = '1';
      nameInput.value = '';
      setTimeout(() => entryError.style.opacity = '0', 2000);
    }
  }
});

// ── STARS ──
const starsContainer = document.getElementById('stars');
const starEls = [];
for (let i = 0; i < 130; i++) {
  const star = document.createElement('div');
  star.className = 'star';
  const size = Math.random() * 2.5 + 0.5;
  star.style.cssText = `
    width: ${size}px; height: ${size}px;
    left: ${Math.random() * 100}%; top: ${Math.random() * 75}%;
    --d: ${2 + Math.random() * 4}s;
    animation-delay: ${Math.random() * 4}s;
    transition: opacity 3s ease;
  `;
  starsContainer.appendChild(star);
  starEls.push(star);
}

// ── SKY PHASES (60s cycle) ──
const phases = [
  { name: 'night',
    bg: ['#0a1520', '#1a2e3b', '#0d1f2d'],
    horizon: 'rgba(26,46,59,0.9)', stars: 1 },
  { name: 'predawn',
    bg: ['#0d1a3a', '#1a2a4e', '#2a1a3e'],
    horizon: 'rgba(60,30,80,0.7)', stars: 0.8 },
  { name: 'sunrise',
    bg: ['#1a1a3a', '#3a1a2e', '#6b2d3e'],
    horizon: 'rgba(200,80,60,0.6)', stars: 0.3 },
  { name: 'golden',
    bg: ['#1a2a4a', '#4a2a1a', '#c05a20'],
    horizon: 'rgba(240,140,40,0.7)', stars: 0.05 },
  { name: 'day',
    bg: ['#0a3a6a', '#1a5a8a', '#2a7aaa'],
    horizon: 'rgba(80,160,200,0.5)', stars: 0 },
  { name: 'afternoon',
    bg: ['#0a2a5a', '#1a4a7a', '#3a8aaa'],
    horizon: 'rgba(60,140,180,0.5)', stars: 0 },
  { name: 'sunset',
    bg: ['#1a1a3a', '#5a2a1a', '#c04a20'],
    horizon: 'rgba(220,100,40,0.7)', stars: 0.1 },
  { name: 'dusk',
    bg: ['#0a1020', '#1a2040', '#2a1a4a'],
    horizon: 'rgba(80,40,100,0.7)', stars: 0.5 },
];

const hero = document.querySelector('.hero');
const horizon = document.querySelector('.horizon');
const CYCLE = 60000;

function lerp(a, b, t) { return a + (b - a) * t; }
function hexToRgb(hex) {
  hex = hex.replace('#','');
  return [parseInt(hex.slice(0,2),16), parseInt(hex.slice(2,4),16), parseInt(hex.slice(4,6),16)];
}
function lerpRgb(a, b, t) {
  return [Math.round(lerp(a[0],b[0],t)), Math.round(lerp(a[1],b[1],t)), Math.round(lerp(a[2],b[2],t))];
}
function rgbStr(c) { return `rgb(${c[0]},${c[1]},${c[2]})`; }
function parseRgba(str) {
  const m = str.match(/[\d.]+/g).map(Number);
  return m.length === 3 ? [...m, 1] : m;
}
function lerpRgba(a, b, t) {
  return [lerp(a[0],b[0],t), lerp(a[1],b[1],t), lerp(a[2],b[2],t), lerp(a[3],b[3],t)];
}
function rgbaStr(c) { return `rgba(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])},${c[3].toFixed(3)})`; }

function frame() {
  const t = (Date.now() % CYCLE) / CYCLE;
  const n = phases.length;
  const phaseF = t * n;
  const i = Math.floor(phaseF) % n;
  const j = (i + 1) % n;
  const blend = phaseF - Math.floor(phaseF);
  const s = blend * blend * (3 - 2 * blend);
  const A = phases[i], B = phases[j];

  const bgColors = [0,1,2].map(k =>
    rgbStr(lerpRgb(hexToRgb(A.bg[k]), hexToRgb(B.bg[k]), s))
  );
  hero.style.background = `linear-gradient(180deg, ${bgColors[0]} 0%, ${bgColors[1]} 50%, ${bgColors[2]} 100%)`;

  const hc = lerpRgba(parseRgba(A.horizon), parseRgba(B.horizon), s);
  horizon.style.background = `linear-gradient(to top, ${rgbaStr(hc)}, transparent)`;

  const starOpacity = lerp(A.stars, B.stars, s);
  if (Math.abs(starOpacity - (frame._lastStarOp || -1)) > 0.008) {
    starEls.forEach(st => { st.style.opacity = starOpacity; });
    frame._lastStarOp = starOpacity;
  }

  requestAnimationFrame(frame);
}

requestAnimationFrame(frame);

// ── SPARKLES ──
const sparkleShapes = ['✦', '★', '✿', '·', '✧', '◆', '✺', '⬟'];
const sparkleColors = [
  '#f0d890','#c9a882','#7fa8b8','#ffffff','#ffd6a5','#d4a8e0','#a8d8c8','#ffb3c6',
  '#b8c4d4','#c4b8d0','#8b4a2e','#7a8c6e'
];

document.addEventListener('mousemove', (e) => {
  if (Math.random() > 0.35) return;
  const el = document.createElement('div');
  el.className = 'sparkle';
  const size = 8 + Math.random() * 16;
  const life = 0.5 + Math.random() * 0.8;
  const color = sparkleColors[Math.floor(Math.random() * sparkleColors.length)];
  const shape = sparkleShapes[Math.floor(Math.random() * sparkleShapes.length)];
  const offsetX = (Math.random() - 0.5) * 24;
  const offsetY = (Math.random() - 0.5) * 24;
  el.style.cssText = `
    left: ${e.clientX + offsetX}px;
    top: ${e.clientY + offsetY}px;
    width: ${size}px; height: ${size}px;
    font-size: ${size * 0.9}px;
    color: ${color};
    line-height: 1;
    text-align: center;
    background: none;
    border-radius: 0;
    --life: ${life}s;
  `;
  el.textContent = shape;
  document.body.appendChild(el);
  setTimeout(() => el.remove(), life * 1000);
});

// ── SCROLL ANIMATIONS ──
const observer = new IntersectionObserver((entries) => {
  entries.forEach(entry => {
    if (entry.isIntersecting) entry.target.classList.add('visible');
  });
}, { threshold: 0.15 });

document.querySelectorAll('.fade-up').forEach(el => observer.observe(el));

// ── DROPDOWN SPARKLES ──
document.querySelectorAll('details > summary').forEach(summary => {
  summary.addEventListener('click', (e) => {
    for (let i = 0; i < 12; i++) {
      const el = document.createElement('div');
      el.className = 'sparkle';
      const size = 8 + Math.random() * 16;
      const life = 0.5 + Math.random() * 4;
      const color = sparkleColors[Math.floor(Math.random() * sparkleColors.length)];
      const shape = sparkleShapes[Math.floor(Math.random() * sparkleShapes.length)];
      const angle = (i / 12) * 360;
      const dist = 20 + Math.random() * 60;
      const dx = Math.cos(angle * Math.PI / 180) * dist;
      const dy = Math.sin(angle * Math.PI / 180) * dist;
      el.style.cssText = `
        left: ${e.clientX + dx}px;
        top: ${e.clientY + dy}px;
        width: ${size}px; height: ${size}px;
        font-size: ${size * 0.9}px;
        color: ${color};
        line-height: 1;
        text-align: center;
        background: none;
        border-radius: 0;
        --life: ${life}s;
      `;
      el.textContent = shape;
      document.body.appendChild(el);
      setTimeout(() => el.remove(), life * 1000);
    }
  });
});

// ── SHOOTING STARS ──
function shootingStar() {
  const el = document.createElement('div');
  const startX = Math.random() * 80;
  const startY = Math.random() * 40;
  el.style.cssText = `
    position: fixed;
    left: ${startX}%;
    top: ${startY}%;
    width: 120px;
    height: 1px;
    background: linear-gradient(90deg, rgba(255,255,255,0.8), transparent);
    transform: rotate(${25 + Math.random() * 20}deg);
    pointer-events: none;
    z-index: 9998;
    animation: shootAcross 0.8s ease-out forwards;
  `;
  document.body.appendChild(el);
  setTimeout(() => el.remove(), 900);
}

const styleEl = document.createElement('style');
styleEl.textContent = `
  @keyframes shootAcross {
    0%   { opacity: 1; transform: rotate(35deg) translateX(0); }
    100% { opacity: 0; transform: rotate(35deg) translateX(200px); }
  }
`;
document.head.appendChild(styleEl);

setInterval(shootingStar, 4000);
</script>

</body>
</html>
